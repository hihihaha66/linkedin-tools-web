name: Patch V2 calculation toggle UI

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/patch-v2-calc-toggle.py'
      - '.github/workflows/patch-v2-calc-toggle.yml'

permissions:
  contents: write

jobs:
  patch-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/v2-break-even-details-toggle
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Apply UI fix
        run: python .github/patch-v2-calc-toggle.py
      - name: Check CSP and inline JavaScript syntax
        shell: bash
        run: |
          python scripts/check-v2-csp.py
          python - <<'PY'
          from pathlib import Path
          html=Path('net-cao-hon-co-that-tot-hon-v2.html').read_text()
          start=html.index('<script>')+len('<script>')
          end=html.index('</script>',start)
          Path('/tmp/v2-inline.js').write_text(html[start:end])
          PY
          node --check /tmp/v2-inline.js
      - name: Install UI test dependencies
        run: |
          npm install --no-save jsdom@24 playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Run existing state/OT smoke tests
        run: |
          node tests/ot-turn4-smoke.js
          node tests/v2-turn6-final-smoke.js
      - name: Start static server
        run: python3 -m http.server 8000 >/tmp/v2-http.log 2>&1 &
      - name: Run responsive + disclosure browser smoke
        run: node tests/v2-turn6-responsive.mjs
      - name: Commit implementation and remove bootstrap files
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/patch-v2-calc-toggle.py .github/workflows/patch-v2-calc-toggle.yml
          git add -- net-cao-hon-co-that-tot-hon-v2.html tests/v2-turn6-responsive.mjs
          git commit -m "Fix V2 calculation disclosure toggle icon"
          git push origin HEAD:fix/v2-break-even-details-toggle
